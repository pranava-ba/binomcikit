# R → Python mapping

This page maps **every function in the original R package
[`proportion`](https://github.com/RajeswaranV/proportion) (v2.1.2, by M. Subbiah
& V. Rajeswaran)** to its equivalent in **`binomcikit`**, the Python port.

Use it to find the Python call for any R function, to jump to the source on
either side, and to see what changed in translation.

## How to read this page

- The **R file** heading (e.g. ``R/101.Confidence_base_n.R``) links to the
  original source on GitHub.
- The **Python file** column links to that module's page in this documentation.
- The **Python function** column links to that function's own API page
  (signature + docstring).
- The **R → Py changes** column notes anything that differs beyond the two
  conventions that apply almost everywhere:
  1. **Naming** — R CamelCase (`ciWD`, `PlotcovpWD`) → Python lower case
     (`ciwd`, `plotcovpwd`).
  2. **Return type** — R `data.frame` → pandas `DataFrame` (plot functions
     return a plotnine `ggplot`).

```{note}
Rows marked *"no standalone function"* are internal R helpers (e.g. `gcovpW`,
`exlim201l`) **folded** into a shared Python engine rather than ported as
separate public functions. Behaviour is preserved; only the packaging differs.
See *"What 'folded' means"* below.
```

## What "folded" means

**Folded** means the R function was not ported as its own standalone Python
function. Instead its logic was **absorbed into a shared helper** that several
Python functions call. Nothing was dropped — the behaviour lives on, just in one
reusable place rather than in a separate named function.

Why it happens: the R package repeats the same computation across every method.
For coverage probability, R has a separate internal helper per method —
`gcovpW` (Wald), `gcovpS` (Score), `gcovpA` (ArcSine), … — and each contains a
near-identical copy of the "simulate *p*, count coverage" loop, differing only
in which interval limits it uses. The Python port writes that loop **once**
(`_coverage_series` / `_coverage_core` in `binomcikit.covp.base_all`); every
method feeds its own limits into it:

```python
# the shared engine — written once, method-agnostic
def _coverage_series(n, lower, upper, hp):
    ...the coverage loop...

# the Wald coverage function just supplies Wald limits:
def covpwd(n, alp, a, b, t1, t2, seed=None):
    df = ciwd(n, alp)                                 # Wald limits
    return _coverage(..., df['LWD'], df['UWD'], ...)  # calls the engine
```

So R's `gcovpW` has no 1:1 Python twin — its guts are the `_coverage_series`
call inside `covpwd`. Only **internal, non-exported** R helpers are folded this
way (coverage/expected-length curve helpers like `gcovpW`, `gexplWD`, and
plumbing like `gintcovpEX202`). Every **public** R function — all 305 exported
ones — got its own real Python function and is never folded.

```{admonition} Reproducibility of simulated results
:class: tip

The evaluation families (coverage `covp*`, expected length `length*`/`expl*`,
empirical Bayes `emperical*`) draw hypothetical *p* from `Beta(a, b)`. R uses
`stats::rbeta`; the Python port uses NumPy's `default_rng`, so these results
match R **in distribution, not draw-for-draw** — each such function takes an
optional `seed=` argument for reproducibility.
```

## Summary

| | Value |
|---|---|
| R source files | 59 (58 contain functions) |
| R function definitions | 470 |
| Exported R functions (public API) | 305 |
| Python functions | 401 numeric + 150 plots |
| Python subpackages | `ci` (1xx), `covp` (2xx), `expl` (3xx), `pconf` (4xx), `err` (5xx), `bayes` (6xx) |

*This page is generated by ``tools/gen_r_to_python_mapping.py`` — do not edit by
hand.*


## 1xx — Confidence Intervals


### [`R/101.Confidence_base_n.R`](https://github.com/RajeswaranV/proportion/blob/master/R/101.Confidence_base_n.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `ciWD` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`ciwd <binomcikit.ci.base_n.ciwd>` | lowercased name; pandas `DataFrame` |
| `ciSC` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`cisc <binomcikit.ci.base_n.cisc>` | lowercased name; pandas `DataFrame` |
| `ciAS` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`cias <binomcikit.ci.base_n.cias>` | lowercased name; pandas `DataFrame` |
| `ciLR` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`cilr <binomcikit.ci.base_n.cilr>` | `x` returned as a column (R port used a MultiIndex) — inconsistency fixed; lowercased name; pandas `DataFrame` |
| `ciEX` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`ciex <binomcikit.ci.base_n.ciex>` | lowercased name; pandas `DataFrame` |
| `lufn101` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`lufn101 <binomcikit.ci.base_n.lufn101>` | internal R helper — ported directly; lowercased name; pandas `DataFrame` |
| `exlim102l` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`exlim102l <binomcikit.ci.base_n.exlim102l>` | internal R helper — ported directly; lowercased name; pandas `DataFrame` |
| `exlim102u` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`exlim102u <binomcikit.ci.base_n.exlim102u>` | internal R helper — ported directly; lowercased name; pandas `DataFrame` |
| `ciTW` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`citw <binomcikit.ci.base_n.citw>` | lowercased name; pandas `DataFrame` |
| `ciLT` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`cilt <binomcikit.ci.base_n.cilt>` | lowercased name; pandas `DataFrame` |
| `ciBA` | {py:mod}`ci.bayes_n <binomcikit.ci.bayes_n>` | {py:func}`ciba <binomcikit.ci.bayes_n.ciba>` | `TeachingDemos::hpd` reimplemented with a SciPy shortest-interval search (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `ciBAD` | — | *(no standalone function)* | Merged into `ciba` (vector-prior path handled by NumPy broadcasting); lowercased name; pandas `DataFrame` |
| `ciAll` | {py:mod}`ci.base_n <binomcikit.ci.base_n>` | {py:func}`ciall <binomcikit.ci.base_n.ciall>` | lowercased name; pandas `DataFrame` |

### [`R/102.Confidence_base_n_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/102.Confidence_base_n_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotciEX` | {py:mod}`ci.base_n_graph <binomcikit.ci.base_n_graph>` | {py:func}`plotciex <binomcikit.ci.base_n_graph.plotciex>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciBA` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotciba <binomcikit.ci.all_graph.plotciba>` | ggplot2 → plotnine; returns a plotnine `ggplot`; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `PlotciBAD` | — | *(no standalone function)* | ggplot2 → plotnine; returns a plotnine `ggplot`; folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `PlotciAll` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotciall <binomcikit.ci.all_graph.plotciall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAllg` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotciallg <binomcikit.ci.all_graph.plotciallg>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciWD` | {py:mod}`ci.base_n_graph <binomcikit.ci.base_n_graph>` | {py:func}`plotciwd <binomcikit.ci.base_n_graph.plotciwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAS` | {py:mod}`ci.base_n_graph <binomcikit.ci.base_n_graph>` | {py:func}`plotcias <binomcikit.ci.base_n_graph.plotcias>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciLR` | {py:mod}`ci.base_n_graph <binomcikit.ci.base_n_graph>` | {py:func}`plotcilr <binomcikit.ci.base_n_graph.plotcilr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciSC` | {py:mod}`ci.base_n_graph <binomcikit.ci.base_n_graph>` | {py:func}`plotcisc <binomcikit.ci.base_n_graph.plotcisc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciTW` | {py:mod}`ci.base_n_graph <binomcikit.ci.base_n_graph>` | {py:func}`plotcitw <binomcikit.ci.base_n_graph.plotcitw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciLT` | {py:mod}`ci.base_n_graph <binomcikit.ci.base_n_graph>` | {py:func}`plotcilt <binomcikit.ci.base_n_graph.plotcilt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/103.ConfidenceIntervals_BASE_n_x.R`](https://github.com/RajeswaranV/proportion/blob/master/R/103.ConfidenceIntervals_BASE_n_x.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `ciWDx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`ciwdx <binomcikit.ci.base_n_x.ciwdx>` | lowercased name; pandas `DataFrame` |
| `ciSCx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`ciscx <binomcikit.ci.base_n_x.ciscx>` | lowercased name; pandas `DataFrame` |
| `ciASx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`ciasx <binomcikit.ci.base_n_x.ciasx>` | lowercased name; pandas `DataFrame` |
| `ciLRx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`cilrx <binomcikit.ci.base_n_x.cilrx>` | lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciEXx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`ciexx <binomcikit.ci.base_n_x.ciexx>` | Root-finding via `scipy.optimize.brentq` (R port called a nonexistent `stats.root_scalar`); lowercased name; pandas `DataFrame` |
| `lufn103` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`lufn103 <binomcikit.ci.base_n_x.lufn103>` | internal R helper — ported directly; lowercased name; pandas `DataFrame` |
| `exlim103l` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`exlim103l <binomcikit.ci.base_n_x.exlim103l>` | internal R helper — ported directly; lowercased name; pandas `DataFrame` |
| `exlim103u` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`exlim103u <binomcikit.ci.base_n_x.exlim103u>` | internal R helper — ported directly; lowercased name; pandas `DataFrame` |
| `ciTWx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`citwx <binomcikit.ci.base_n_x.citwx>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciLTx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`ciltx <binomcikit.ci.base_n_x.ciltx>` | lowercased name; pandas `DataFrame` |
| `ciBAx` | {py:mod}`ci.bayes_n_x <binomcikit.ci.bayes_n_x>` | {py:func}`cibax <binomcikit.ci.bayes_n_x.cibax>` | `TeachingDemos::hpd` reimplemented with SciPy (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `ciBADx` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciAllx` | {py:mod}`ci.base_n_x <binomcikit.ci.base_n_x>` | {py:func}`ciallx <binomcikit.ci.base_n_x.ciallx>` | Newly added — was missing from the earlier partial port; lowercased name; pandas `DataFrame` |

### [`R/104.ConfidenceIntervals_BASE_n_x_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/104.ConfidenceIntervals_BASE_n_x_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotciEXx` | {py:mod}`ci.base_n_x_graph <binomcikit.ci.base_n_x_graph>` | {py:func}`plotciexx <binomcikit.ci.base_n_x_graph.plotciexx>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAllx` | {py:mod}`ci.base_n_x_graph <binomcikit.ci.base_n_x_graph>` | {py:func}`plotciallx <binomcikit.ci.base_n_x_graph.plotciallx>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAllxg` | {py:mod}`ci.base_n_x_graph <binomcikit.ci.base_n_x_graph>` | {py:func}`plotciallxg <binomcikit.ci.base_n_x_graph.plotciallxg>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/111.ConfidenceIntervals_ADJ_n.R`](https://github.com/RajeswaranV/proportion/blob/master/R/111.ConfidenceIntervals_ADJ_n.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `ciAWD` | {py:mod}`ci.adj_n <binomcikit.ci.adj_n>` | {py:func}`ciawd <binomcikit.ci.adj_n.ciawd>` | lowercased name; pandas `DataFrame` |
| `ciASC` | {py:mod}`ci.adj_n <binomcikit.ci.adj_n>` | {py:func}`ciasc <binomcikit.ci.adj_n.ciasc>` | lowercased name; pandas `DataFrame` |
| `ciAAS` | {py:mod}`ci.adj_n <binomcikit.ci.adj_n>` | {py:func}`ciaas <binomcikit.ci.adj_n.ciaas>` | lowercased name; pandas `DataFrame` |
| `ciALR` | {py:mod}`ci.adj_n <binomcikit.ci.adj_n>` | {py:func}`cialr <binomcikit.ci.adj_n.cialr>` | lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciATW` | {py:mod}`ci.adj_n <binomcikit.ci.adj_n>` | {py:func}`ciatw <binomcikit.ci.adj_n.ciatw>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciALT` | {py:mod}`ci.adj_n <binomcikit.ci.adj_n>` | {py:func}`cialt <binomcikit.ci.adj_n.cialt>` | lowercased name; pandas `DataFrame` |
| `ciAAll` | {py:mod}`ci.adj_n <binomcikit.ci.adj_n>` | {py:func}`ciaall <binomcikit.ci.adj_n.ciaall>` | lowercased name; pandas `DataFrame` |

### [`R/112.ConfidenceIntervals_ADJ_n_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/112.ConfidenceIntervals_ADJ_n_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotciAAll` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotciaall <binomcikit.ci.all_graph.plotciaall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAAllg` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotciaallg <binomcikit.ci.all_graph.plotciaallg>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAWD` | {py:mod}`ci.adj_n_graph <binomcikit.ci.adj_n_graph>` | {py:func}`plotciawd <binomcikit.ci.adj_n_graph.plotciawd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAAS` | {py:mod}`ci.adj_n_graph <binomcikit.ci.adj_n_graph>` | {py:func}`plotciaas <binomcikit.ci.adj_n_graph.plotciaas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciALR` | {py:mod}`ci.adj_n_graph <binomcikit.ci.adj_n_graph>` | {py:func}`plotcialr <binomcikit.ci.adj_n_graph.plotcialr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciASC` | {py:mod}`ci.adj_n_graph <binomcikit.ci.adj_n_graph>` | {py:func}`plotciasc <binomcikit.ci.adj_n_graph.plotciasc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciALT` | {py:mod}`ci.adj_n_graph <binomcikit.ci.adj_n_graph>` | {py:func}`plotcialt <binomcikit.ci.adj_n_graph.plotcialt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciATW` | {py:mod}`ci.adj_n_graph <binomcikit.ci.adj_n_graph>` | {py:func}`plotciatw <binomcikit.ci.adj_n_graph.plotciatw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/113.ConfidenceIntervals_ADJ_n_x.R`](https://github.com/RajeswaranV/proportion/blob/master/R/113.ConfidenceIntervals_ADJ_n_x.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `ciAWDx` | {py:mod}`ci.adj_n_x <binomcikit.ci.adj_n_x>` | {py:func}`ciawdx <binomcikit.ci.adj_n_x.ciawdx>` | lowercased name; pandas `DataFrame` |
| `ciASCx` | {py:mod}`ci.adj_n_x <binomcikit.ci.adj_n_x>` | {py:func}`ciascx <binomcikit.ci.adj_n_x.ciascx>` | lowercased name; pandas `DataFrame` |
| `ciAASx` | {py:mod}`ci.adj_n_x <binomcikit.ci.adj_n_x>` | {py:func}`ciaasx <binomcikit.ci.adj_n_x.ciaasx>` | lowercased name; pandas `DataFrame` |
| `ciALRx` | {py:mod}`ci.adj_n_x <binomcikit.ci.adj_n_x>` | {py:func}`cialrx <binomcikit.ci.adj_n_x.cialrx>` | lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciATWx` | {py:mod}`ci.adj_n_x <binomcikit.ci.adj_n_x>` | {py:func}`ciatwx <binomcikit.ci.adj_n_x.ciatwx>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciALTx` | {py:mod}`ci.adj_n_x <binomcikit.ci.adj_n_x>` | {py:func}`cialtx <binomcikit.ci.adj_n_x.cialtx>` | lowercased name; pandas `DataFrame` |
| `ciAAllx` | {py:mod}`ci.adj_n_x <binomcikit.ci.adj_n_x>` | {py:func}`ciaallx <binomcikit.ci.adj_n_x.ciaallx>` | lowercased name; pandas `DataFrame` |
| `PlotciAAllx` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotciaallx <binomcikit.ci.all_graph.plotciaallx>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciAAllxg` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotciaallxg <binomcikit.ci.all_graph.plotciaallxg>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/121.ConfidenceIntervals_CC_n.R`](https://github.com/RajeswaranV/proportion/blob/master/R/121.ConfidenceIntervals_CC_n.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `ciCWD` | {py:mod}`ci.cc_n <binomcikit.ci.cc_n>` | {py:func}`cicwd <binomcikit.ci.cc_n.cicwd>` | lowercased name; pandas `DataFrame` |
| `ciCSC` | {py:mod}`ci.cc_n <binomcikit.ci.cc_n>` | {py:func}`cicsc <binomcikit.ci.cc_n.cicsc>` | lowercased name; pandas `DataFrame` |
| `ciCAS` | {py:mod}`ci.cc_n <binomcikit.ci.cc_n>` | {py:func}`cicas <binomcikit.ci.cc_n.cicas>` | lowercased name; pandas `DataFrame` |
| `ciCLT` | {py:mod}`ci.cc_n <binomcikit.ci.cc_n>` | {py:func}`ciclt <binomcikit.ci.cc_n.ciclt>` | lowercased name; pandas `DataFrame` |
| `lgiti` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciCTW` | {py:mod}`ci.cc_n <binomcikit.ci.cc_n>` | {py:func}`cictw <binomcikit.ci.cc_n.cictw>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciCAll` | {py:mod}`ci.cc_n <binomcikit.ci.cc_n>` | {py:func}`cicall <binomcikit.ci.cc_n.cicall>` | lowercased name; pandas `DataFrame` |

### [`R/122.ConfidenceIntervals_CC_n_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/122.ConfidenceIntervals_CC_n_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotciCAll` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotcicall <binomcikit.ci.all_graph.plotcicall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciCAllg` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotcicallg <binomcikit.ci.all_graph.plotcicallg>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciCWD` | {py:mod}`ci.cc_n_graph <binomcikit.ci.cc_n_graph>` | {py:func}`plotcicwd <binomcikit.ci.cc_n_graph.plotcicwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciCAS` | {py:mod}`ci.cc_n_graph <binomcikit.ci.cc_n_graph>` | {py:func}`plotcicas <binomcikit.ci.cc_n_graph.plotcicas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciCSC` | {py:mod}`ci.cc_n_graph <binomcikit.ci.cc_n_graph>` | {py:func}`plotcicsc <binomcikit.ci.cc_n_graph.plotcicsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciCLT` | {py:mod}`ci.cc_n_graph <binomcikit.ci.cc_n_graph>` | {py:func}`plotciclt <binomcikit.ci.cc_n_graph.plotciclt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciCTW` | {py:mod}`ci.cc_n_graph <binomcikit.ci.cc_n_graph>` | {py:func}`plotcictw <binomcikit.ci.cc_n_graph.plotcictw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/123.ConfidenceIntervals_CC_n_x.R`](https://github.com/RajeswaranV/proportion/blob/master/R/123.ConfidenceIntervals_CC_n_x.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `ciCWDx` | {py:mod}`ci.cc_n_x <binomcikit.ci.cc_n_x>` | {py:func}`cicwdx <binomcikit.ci.cc_n_x.cicwdx>` | lowercased name; pandas `DataFrame` |
| `ciCSCx` | {py:mod}`ci.cc_n_x <binomcikit.ci.cc_n_x>` | {py:func}`cicscx <binomcikit.ci.cc_n_x.cicscx>` | lowercased name; pandas `DataFrame` |
| `ciCASx` | {py:mod}`ci.cc_n_x <binomcikit.ci.cc_n_x>` | {py:func}`cicasx <binomcikit.ci.cc_n_x.cicasx>` | lowercased name; pandas `DataFrame` |
| `ciCLTx` | {py:mod}`ci.cc_n_x <binomcikit.ci.cc_n_x>` | {py:func}`cicltx <binomcikit.ci.cc_n_x.cicltx>` | lowercased name; pandas `DataFrame` |
| `lgiti` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciCTWx` | {py:mod}`ci.cc_n_x <binomcikit.ci.cc_n_x>` | {py:func}`cictwx <binomcikit.ci.cc_n_x.cictwx>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `ciCAllx` | {py:mod}`ci.cc_n_x <binomcikit.ci.cc_n_x>` | {py:func}`cicallx <binomcikit.ci.cc_n_x.cicallx>` | lowercased name; pandas `DataFrame` |
| `PlotciCAllx` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotcicallx <binomcikit.ci.all_graph.plotcicallx>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotciCAllxg` | {py:mod}`ci.all_graph <binomcikit.ci.all_graph>` | {py:func}`plotcicallxg <binomcikit.ci.all_graph.plotcicallxg>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

## 2xx — Coverage Probability


### [`R/201.CoverageProb_BASE_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/201.CoverageProb_BASE_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `covpWD` | {py:mod}`covp.base_all <binomcikit.covp.base_all>` | {py:func}`covpwd <binomcikit.covp.base_all.covpwd>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpSC` | {py:mod}`covp.base_all <binomcikit.covp.base_all>` | {py:func}`covpsc <binomcikit.covp.base_all.covpsc>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpAS` | {py:mod}`covp.base_all <binomcikit.covp.base_all>` | {py:func}`covpas <binomcikit.covp.base_all.covpas>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpLT` | {py:mod}`covp.base_all <binomcikit.covp.base_all>` | {py:func}`covplt <binomcikit.covp.base_all.covplt>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpTW` | {py:mod}`covp.base_all <binomcikit.covp.base_all>` | {py:func}`covptw <binomcikit.covp.base_all.covptw>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `covpLR` | {py:mod}`covp.base_all <binomcikit.covp.base_all>` | {py:func}`covplr <binomcikit.covp.base_all.covplr>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `covpEX` | {py:mod}`covp.exact_bayes <binomcikit.covp.exact_bayes>` | {py:func}`covpex <binomcikit.covp.exact_bayes.covpex>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `oldEX201` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `exlim201l` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `exlim201u` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `covpBA` | {py:mod}`covp.exact_bayes <binomcikit.covp.exact_bayes>` | {py:func}`covpba <binomcikit.covp.exact_bayes.covpba>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `covpAll` | {py:mod}`covp.base_all <binomcikit.covp.base_all>` | {py:func}`covpall <binomcikit.covp.base_all.covpall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |

### [`R/202.CoverageProb_BASE_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/202.CoverageProb_BASE_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotcovpEX` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpex <binomcikit.covp.plots.plotcovpex>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpBA` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpba <binomcikit.covp.plots.plotcovpba>` | ggplot2 → plotnine; returns a plotnine `ggplot`; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `PlotcovpAll` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpall <binomcikit.covp.plots.plotcovpall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpWD` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpwd <binomcikit.covp.plots.plotcovpwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpAS` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpas <binomcikit.covp.plots.plotcovpas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpLR` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovplr <binomcikit.covp.plots.plotcovplr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpSC` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpsc <binomcikit.covp.plots.plotcovpsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpLT` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovplt <binomcikit.covp.plots.plotcovplt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpTW` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovptw <binomcikit.covp.plots.plotcovptw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/203.CoverageProb_BASE_internal.R`](https://github.com/RajeswaranV/proportion/blob/master/R/203.CoverageProb_BASE_internal.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `gcovpW` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpS` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpA` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpLT` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpTW` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpL` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpEX` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gintcovpEX202` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `exlim202l` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `exlim202u` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |

### [`R/212.CoverageProb_ADJ_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/212.CoverageProb_ADJ_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `covpAWD` | {py:mod}`covp.adj_all <binomcikit.covp.adj_all>` | {py:func}`covpawd <binomcikit.covp.adj_all.covpawd>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpASC` | {py:mod}`covp.adj_all <binomcikit.covp.adj_all>` | {py:func}`covpasc <binomcikit.covp.adj_all.covpasc>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpAAS` | {py:mod}`covp.adj_all <binomcikit.covp.adj_all>` | {py:func}`covpaas <binomcikit.covp.adj_all.covpaas>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpALT` | {py:mod}`covp.adj_all <binomcikit.covp.adj_all>` | {py:func}`covpalt <binomcikit.covp.adj_all.covpalt>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpATW` | {py:mod}`covp.adj_all <binomcikit.covp.adj_all>` | {py:func}`covpatw <binomcikit.covp.adj_all.covpatw>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `covpALR` | {py:mod}`covp.adj_all <binomcikit.covp.adj_all>` | {py:func}`covpalr <binomcikit.covp.adj_all.covpalr>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `covpAAll` | {py:mod}`covp.adj_all <binomcikit.covp.adj_all>` | {py:func}`covpaall <binomcikit.covp.adj_all.covpaall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |

### [`R/213.CoverageProb_ADJ_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/213.CoverageProb_ADJ_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotcovpAAll` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpaall <binomcikit.covp.plots.plotcovpaall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpAWD` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpawd <binomcikit.covp.plots.plotcovpawd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpAAS` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpaas <binomcikit.covp.plots.plotcovpaas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpALR` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpalr <binomcikit.covp.plots.plotcovpalr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpASC` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpasc <binomcikit.covp.plots.plotcovpasc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpALT` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpalt <binomcikit.covp.plots.plotcovpalt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpATW` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpatw <binomcikit.covp.plots.plotcovpatw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/214.CoverageProb_ADJ_internal.R`](https://github.com/RajeswaranV/proportion/blob/master/R/214.CoverageProb_ADJ_internal.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `gcovpAWD` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpASC` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpAAS` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpALT` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpATW` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpALR` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |

### [`R/221.CoverageProb_CC_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/221.CoverageProb_CC_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `covpCWD` | {py:mod}`covp.cc_all <binomcikit.covp.cc_all>` | {py:func}`covpcwd <binomcikit.covp.cc_all.covpcwd>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpCSC` | {py:mod}`covp.cc_all <binomcikit.covp.cc_all>` | {py:func}`covpcsc <binomcikit.covp.cc_all.covpcsc>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpCAS` | {py:mod}`covp.cc_all <binomcikit.covp.cc_all>` | {py:func}`covpcas <binomcikit.covp.cc_all.covpcas>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `covpCLT` | {py:mod}`covp.cc_all <binomcikit.covp.cc_all>` | {py:func}`covpclt <binomcikit.covp.cc_all.covpclt>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lgiti` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `covpCTW` | {py:mod}`covp.cc_all <binomcikit.covp.cc_all>` | {py:func}`covpctw <binomcikit.covp.cc_all.covpctw>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `covpCAll` | {py:mod}`covp.cc_all <binomcikit.covp.cc_all>` | {py:func}`covpcall <binomcikit.covp.cc_all.covpcall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |

### [`R/222.CoverageProb_CC_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/222.CoverageProb_CC_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotcovpCAll` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpcall <binomcikit.covp.plots.plotcovpcall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpCWD` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpcwd <binomcikit.covp.plots.plotcovpcwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpCAS` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpcas <binomcikit.covp.plots.plotcovpcas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpCSC` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpcsc <binomcikit.covp.plots.plotcovpcsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpCLT` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpclt <binomcikit.covp.plots.plotcovpclt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotcovpCTW` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpctw <binomcikit.covp.plots.plotcovpctw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/223.CoverageProb_CC_internal.R`](https://github.com/RajeswaranV/proportion/blob/master/R/223.CoverageProb_CC_internal.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `gcovpCWD` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpCSC` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpCAS` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpCLT` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gcovpCTW` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |

### [`R/224.CoverageProb_GENERAL_GIVEN_p.R`](https://github.com/RajeswaranV/proportion/blob/master/R/224.CoverageProb_GENERAL_GIVEN_p.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `covpGEN` | {py:mod}`covp.general <binomcikit.covp.general>` | {py:func}`covpgen <binomcikit.covp.general.covpgen>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `PlotcovpGEN` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpgen <binomcikit.covp.plots.plotcovpgen>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/225.CoverageProb_GENERAL_SIMULATEDp.R`](https://github.com/RajeswaranV/proportion/blob/master/R/225.CoverageProb_GENERAL_SIMULATEDp.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `covpSIM` | {py:mod}`covp.general <binomcikit.covp.general>` | {py:func}`covpsim <binomcikit.covp.general.covpsim>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `PlotcovpSIM` | {py:mod}`covp.plots <binomcikit.covp.plots>` | {py:func}`plotcovpsim <binomcikit.covp.plots.plotcovpsim>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

## 3xx — Expected Length


### [`R/301.Expec_Leng_BASE_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/301.Expec_Leng_BASE_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `lengthWD` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthwd <binomcikit.expl.base_all.lengthwd>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthSC` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthsc <binomcikit.expl.base_all.lengthsc>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthAS` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthas <binomcikit.expl.base_all.lengthas>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthLT` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthlt <binomcikit.expl.base_all.lengthlt>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthTW` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthtw <binomcikit.expl.base_all.lengthtw>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `lengthLR` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthlr <binomcikit.expl.base_all.lengthlr>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `lengthEX` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthex <binomcikit.expl.base_all.lengthex>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `glengthEX301` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `exlim301l` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `exlim301u` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `lengthBA` | {py:mod}`expl.bayes <binomcikit.expl.bayes>` | {py:func}`lengthba <binomcikit.expl.bayes.lengthba>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `lengthAll` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`lengthall <binomcikit.expl.base_all.lengthall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `explAll` | {py:mod}`expl.base_all <binomcikit.expl.base_all>` | {py:func}`explall <binomcikit.expl.base_all.explall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |

### [`R/302.Expec_Leng_BASE_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/302.Expec_Leng_BASE_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotexplEX` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplex <binomcikit.expl.plots.plotexplex>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `gexplEX` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `ncf302` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `exlim302l` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `exlim302u` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `PlotexplBA` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplba <binomcikit.expl.plots.plotexplba>` | ggplot2 → plotnine; returns a plotnine `ggplot`; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `PlotexplAll` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplall <binomcikit.expl.plots.plotexplall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplWD` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplwd <binomcikit.expl.plots.plotexplwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplSC` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplsc <binomcikit.expl.plots.plotexplsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplAS` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplas <binomcikit.expl.plots.plotexplas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplLT` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexpllt <binomcikit.expl.plots.plotexpllt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplTW` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexpltw <binomcikit.expl.plots.plotexpltw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplLR` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexpllr <binomcikit.expl.plots.plotexpllr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/303.Sum_Leng_BASE_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/303.Sum_Leng_BASE_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotlengthAll` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthall <binomcikit.expl.plots.plotlengthall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthEX` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthex <binomcikit.expl.plots.plotlengthex>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthBA` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthba <binomcikit.expl.plots.plotlengthba>` | ggplot2 → plotnine; returns a plotnine `ggplot`; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `PlotlengthWD` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthwd <binomcikit.expl.plots.plotlengthwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthSC` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthsc <binomcikit.expl.plots.plotlengthsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthAS` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthas <binomcikit.expl.plots.plotlengthas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthLT` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthlt <binomcikit.expl.plots.plotlengthlt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthTW` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthtw <binomcikit.expl.plots.plotlengthtw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthLR` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthlr <binomcikit.expl.plots.plotlengthlr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/304.Expec_Leng_BASE_internal.R`](https://github.com/RajeswaranV/proportion/blob/master/R/304.Expec_Leng_BASE_internal.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `gexplWD` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplSC` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplAS` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplLT` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplTW` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplLR` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |

### [`R/311.Expec_Leng_ADJ_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/311.Expec_Leng_ADJ_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `lengthAWD` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`lengthawd <binomcikit.expl.adj_all.lengthawd>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthASC` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`lengthasc <binomcikit.expl.adj_all.lengthasc>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthAAS` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`lengthaas <binomcikit.expl.adj_all.lengthaas>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthALT` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`lengthalt <binomcikit.expl.adj_all.lengthalt>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthATW` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`lengthatw <binomcikit.expl.adj_all.lengthatw>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `lengthALR` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`lengthalr <binomcikit.expl.adj_all.lengthalr>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `lengthAAll` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`lengthaall <binomcikit.expl.adj_all.lengthaall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `explAAll` | {py:mod}`expl.adj_all <binomcikit.expl.adj_all>` | {py:func}`explaall <binomcikit.expl.adj_all.explaall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |

### [`R/312.Expec_Leng_ADJ_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/312.Expec_Leng_ADJ_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotexplAAll` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplaall <binomcikit.expl.plots.plotexplaall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplAWD` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplawd <binomcikit.expl.plots.plotexplawd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplASC` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplasc <binomcikit.expl.plots.plotexplasc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplATW` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplatw <binomcikit.expl.plots.plotexplatw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplALT` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplalt <binomcikit.expl.plots.plotexplalt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplAAS` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplaas <binomcikit.expl.plots.plotexplaas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplALR` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplalr <binomcikit.expl.plots.plotexplalr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/313.Sum_Leng_ADJ_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/313.Sum_Leng_ADJ_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotlengthAAll` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthaall <binomcikit.expl.plots.plotlengthaall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthAWD` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthawd <binomcikit.expl.plots.plotlengthawd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthASC` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthasc <binomcikit.expl.plots.plotlengthasc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthATW` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthatw <binomcikit.expl.plots.plotlengthatw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthALT` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthalt <binomcikit.expl.plots.plotlengthalt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthAAS` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthaas <binomcikit.expl.plots.plotlengthaas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthALR` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthalr <binomcikit.expl.plots.plotlengthalr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/314.Expec_Leng_ADJ_internal.R`](https://github.com/RajeswaranV/proportion/blob/master/R/314.Expec_Leng_ADJ_internal.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `gexplAWD` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplASC` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplAAS` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplALT` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplATW` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplALR` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |

### [`R/321.Expec_Leng_CC_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/321.Expec_Leng_CC_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `lengthCWD` | {py:mod}`expl.cc_all <binomcikit.expl.cc_all>` | {py:func}`lengthcwd <binomcikit.expl.cc_all.lengthcwd>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthCSC` | {py:mod}`expl.cc_all <binomcikit.expl.cc_all>` | {py:func}`lengthcsc <binomcikit.expl.cc_all.lengthcsc>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthCAS` | {py:mod}`expl.cc_all <binomcikit.expl.cc_all>` | {py:func}`lengthcas <binomcikit.expl.cc_all.lengthcas>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lengthCLT` | {py:mod}`expl.cc_all <binomcikit.expl.cc_all>` | {py:func}`lengthclt <binomcikit.expl.cc_all.lengthclt>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `lgiti` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `lengthCTW` | {py:mod}`expl.cc_all <binomcikit.expl.cc_all>` | {py:func}`lengthctw <binomcikit.expl.cc_all.lengthctw>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `lengthCAll` | {py:mod}`expl.cc_all <binomcikit.expl.cc_all>` | {py:func}`lengthcall <binomcikit.expl.cc_all.lengthcall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `explCAll` | {py:mod}`expl.cc_all <binomcikit.expl.cc_all>` | {py:func}`explcall <binomcikit.expl.cc_all.explcall>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |

### [`R/322.Expec_Leng_CC_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/322.Expec_Leng_CC_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotexplCAll` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplcall <binomcikit.expl.plots.plotexplcall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplCWD` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplcwd <binomcikit.expl.plots.plotexplcwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplCSC` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplcsc <binomcikit.expl.plots.plotexplcsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplCAS` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplcas <binomcikit.expl.plots.plotexplcas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplCLT` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplclt <binomcikit.expl.plots.plotexplclt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotexplCTW` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplctw <binomcikit.expl.plots.plotexplctw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/323.Sum_Leng_CC_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/323.Sum_Leng_CC_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotlengthCAll` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthcall <binomcikit.expl.plots.plotlengthcall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthCWD` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthcwd <binomcikit.expl.plots.plotlengthcwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthCSC` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthcsc <binomcikit.expl.plots.plotlengthcsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthCAS` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthcas <binomcikit.expl.plots.plotlengthcas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthCLT` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthclt <binomcikit.expl.plots.plotlengthclt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotlengthCTW` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthctw <binomcikit.expl.plots.plotlengthctw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/325.Expec_Leng_CC_internal.R`](https://github.com/RajeswaranV/proportion/blob/master/R/325.Expec_Leng_CC_internal.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `gexplCWD` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplCSC` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplCAS` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplCLT` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `gexplCTW` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |

### [`R/326.Expec_Leng_GENERAL_SIMULATEDp.R`](https://github.com/RajeswaranV/proportion/blob/master/R/326.Expec_Leng_GENERAL_SIMULATEDp.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotexplSIM` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplsim <binomcikit.expl.plots.plotexplsim>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/327.Expec_Leng_GENERAL_GIVENp.R`](https://github.com/RajeswaranV/proportion/blob/master/R/327.Expec_Leng_GENERAL_GIVENp.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotexplGEN` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotexplgen <binomcikit.expl.plots.plotexplgen>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/328.Sum_Leng_GENERAL_SIM.R`](https://github.com/RajeswaranV/proportion/blob/master/R/328.Sum_Leng_GENERAL_SIM.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `lengthGEN` | {py:mod}`expl.general <binomcikit.expl.general>` | {py:func}`lengthgen <binomcikit.expl.general.lengthgen>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `PlotlengthGEN` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthgen <binomcikit.expl.plots.plotlengthgen>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `lengthSIM` | {py:mod}`expl.general <binomcikit.expl.general>` | {py:func}`lengthsim <binomcikit.expl.general.lengthsim>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; lowercased name; pandas `DataFrame` |
| `PlotlengthSIM` | {py:mod}`expl.plots <binomcikit.expl.plots>` | {py:func}`plotlengthsim <binomcikit.expl.plots.plotlengthsim>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

## 4xx — p-Confidence & p-Bias


### [`R/401.p-Confidence_p-Bias_BASE_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/401.p-Confidence_p-Bias_BASE_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `pCOpBIWD` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbiwd <binomcikit.pconf.base_all.pcopbiwd>` | lowercased name; pandas `DataFrame` |
| `pCOpBISC` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbisc <binomcikit.pconf.base_all.pcopbisc>` | lowercased name; pandas `DataFrame` |
| `pCOpBIAS` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbias <binomcikit.pconf.base_all.pcopbias>` | lowercased name; pandas `DataFrame` |
| `pCOpBILT` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbilt <binomcikit.pconf.base_all.pcopbilt>` | lowercased name; pandas `DataFrame` |
| `pCOpBITW` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbitw <binomcikit.pconf.base_all.pcopbitw>` | lowercased name; pandas `DataFrame` |
| `pCOpBILR` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbilr <binomcikit.pconf.base_all.pcopbilr>` | lowercased name; pandas `DataFrame` |
| `pCOpBIEX` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbiex <binomcikit.pconf.base_all.pcopbiex>` | lowercased name; pandas `DataFrame` |
| `slu401` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `pcb401` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `exlim401l` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `exlim401u` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `pCOpBIBA` | {py:mod}`pconf.bayes <binomcikit.pconf.bayes>` | {py:func}`pcopbiba <binomcikit.pconf.bayes.pcopbiba>` | uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `pCOpBIAll` | {py:mod}`pconf.base_all <binomcikit.pconf.base_all>` | {py:func}`pcopbiall <binomcikit.pconf.base_all.pcopbiall>` | lowercased name; pandas `DataFrame` |

### [`R/402.p-Confidence_p-Bias_BASE_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/402.p-Confidence_p-Bias_BASE_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotpCOpBIEX` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiex <binomcikit.pconf.plots.plotpcopbiex>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIBA` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiba <binomcikit.pconf.plots.plotpcopbiba>` | ggplot2 → plotnine; returns a plotnine `ggplot`; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `PlotpCOpBIAll` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiall <binomcikit.pconf.plots.plotpcopbiall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIWD` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiwd <binomcikit.pconf.plots.plotpcopbiwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBISC` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbisc <binomcikit.pconf.plots.plotpcopbisc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIAS` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbias <binomcikit.pconf.plots.plotpcopbias>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBILT` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbilt <binomcikit.pconf.plots.plotpcopbilt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBITW` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbitw <binomcikit.pconf.plots.plotpcopbitw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBILR` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbilr <binomcikit.pconf.plots.plotpcopbilr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/411.p-Confidence_p-Bias_ADJ_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/411.p-Confidence_p-Bias_ADJ_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `pCOpBIAWD` | {py:mod}`pconf.adj_all <binomcikit.pconf.adj_all>` | {py:func}`pcopbiawd <binomcikit.pconf.adj_all.pcopbiawd>` | lowercased name; pandas `DataFrame` |
| `pCOpBIASC` | {py:mod}`pconf.adj_all <binomcikit.pconf.adj_all>` | {py:func}`pcopbiasc <binomcikit.pconf.adj_all.pcopbiasc>` | lowercased name; pandas `DataFrame` |
| `pCOpBIAAS` | {py:mod}`pconf.adj_all <binomcikit.pconf.adj_all>` | {py:func}`pcopbiaas <binomcikit.pconf.adj_all.pcopbiaas>` | lowercased name; pandas `DataFrame` |
| `pCOpBIALT` | {py:mod}`pconf.adj_all <binomcikit.pconf.adj_all>` | {py:func}`pcopbialt <binomcikit.pconf.adj_all.pcopbialt>` | lowercased name; pandas `DataFrame` |
| `pCOpBIATW` | {py:mod}`pconf.adj_all <binomcikit.pconf.adj_all>` | {py:func}`pcopbiatw <binomcikit.pconf.adj_all.pcopbiatw>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `pCOpBIALR` | {py:mod}`pconf.adj_all <binomcikit.pconf.adj_all>` | {py:func}`pcopbialr <binomcikit.pconf.adj_all.pcopbialr>` | lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `pCOpBIAAll` | {py:mod}`pconf.adj_all <binomcikit.pconf.adj_all>` | {py:func}`pcopbiaall <binomcikit.pconf.adj_all.pcopbiaall>` | lowercased name; pandas `DataFrame` |

### [`R/412.p-Confidence_p-Bias_ADJ_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/412.p-Confidence_p-Bias_ADJ_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotpCOpBIAAll` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiaall <binomcikit.pconf.plots.plotpcopbiaall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIAWD` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiawd <binomcikit.pconf.plots.plotpcopbiawd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIALR` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbialr <binomcikit.pconf.plots.plotpcopbialr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIATW` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiatw <binomcikit.pconf.plots.plotpcopbiatw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIALT` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbialt <binomcikit.pconf.plots.plotpcopbialt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIASC` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiasc <binomcikit.pconf.plots.plotpcopbiasc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBIAAS` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiaas <binomcikit.pconf.plots.plotpcopbiaas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/421.p-Confidence_p-Bias_CC_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/421.p-Confidence_p-Bias_CC_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `pCOpBICWD` | {py:mod}`pconf.cc_all <binomcikit.pconf.cc_all>` | {py:func}`pcopbicwd <binomcikit.pconf.cc_all.pcopbicwd>` | lowercased name; pandas `DataFrame` |
| `pCOpBICSC` | {py:mod}`pconf.cc_all <binomcikit.pconf.cc_all>` | {py:func}`pcopbicsc <binomcikit.pconf.cc_all.pcopbicsc>` | lowercased name; pandas `DataFrame` |
| `pCOpBICAS` | {py:mod}`pconf.cc_all <binomcikit.pconf.cc_all>` | {py:func}`pcopbicas <binomcikit.pconf.cc_all.pcopbicas>` | lowercased name; pandas `DataFrame` |
| `pCOpBICLT` | {py:mod}`pconf.cc_all <binomcikit.pconf.cc_all>` | {py:func}`pcopbiclt <binomcikit.pconf.cc_all.pcopbiclt>` | lowercased name; pandas `DataFrame` |
| `lgiti` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `pCOpBICTW` | {py:mod}`pconf.cc_all <binomcikit.pconf.cc_all>` | {py:func}`pcopbictw <binomcikit.pconf.cc_all.pcopbictw>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `pCOpBICAll` | {py:mod}`pconf.cc_all <binomcikit.pconf.cc_all>` | {py:func}`pcopbicall <binomcikit.pconf.cc_all.pcopbicall>` | lowercased name; pandas `DataFrame` |

### [`R/422.p-Confidence_p-Bias_CC_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/422.p-Confidence_p-Bias_CC_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PlotpCOpBICAll` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbicall <binomcikit.pconf.plots.plotpcopbicall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBICWD` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbicwd <binomcikit.pconf.plots.plotpcopbicwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBICAS` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbicas <binomcikit.pconf.plots.plotpcopbicas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBICTW` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbictw <binomcikit.pconf.plots.plotpcopbictw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBICLT` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbiclt <binomcikit.pconf.plots.plotpcopbiclt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PlotpCOpBICSC` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbicsc <binomcikit.pconf.plots.plotpcopbicsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/423.p-Confidence_p-Bias_GENERAL.R`](https://github.com/RajeswaranV/proportion/blob/master/R/423.p-Confidence_p-Bias_GENERAL.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `pCOpBIGEN` | {py:mod}`pconf.general <binomcikit.pconf.general>` | {py:func}`pcopbigen <binomcikit.pconf.general.pcopbigen>` | lowercased name; pandas `DataFrame` |
| `PlotpCOpBIGEN` | {py:mod}`pconf.plots <binomcikit.pconf.plots>` | {py:func}`plotpcopbigen <binomcikit.pconf.plots.plotpcopbigen>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

## 5xx — Error & Failure


### [`R/501.Error-Failure_LimitBased_BASE_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/501.Error-Failure_LimitBased_BASE_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `errWD` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`errwd <binomcikit.err.base_all.errwd>` | lowercased name; pandas `DataFrame` |
| `errSC` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`errsc <binomcikit.err.base_all.errsc>` | lowercased name; pandas `DataFrame` |
| `errAS` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`erras <binomcikit.err.base_all.erras>` | lowercased name; pandas `DataFrame` |
| `errLT` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`errlt <binomcikit.err.base_all.errlt>` | lowercased name; pandas `DataFrame` |
| `errTW` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`errtw <binomcikit.err.base_all.errtw>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `errLR` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`errlr <binomcikit.err.base_all.errlr>` | lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `errEX` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`errex <binomcikit.err.base_all.errex>` | lowercased name; pandas `DataFrame` |
| `gerrEX501` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `exlim501l` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `exlim501u` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `errBA` | {py:mod}`err.bayes <binomcikit.err.bayes>` | {py:func}`errba <binomcikit.err.bayes.errba>` | uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `errAll` | {py:mod}`err.base_all <binomcikit.err.base_all>` | {py:func}`errall <binomcikit.err.base_all.errall>` | lowercased name; pandas `DataFrame` |

### [`R/502.Error-Failure_LimitBased_BASE_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/502.Error-Failure_LimitBased_BASE_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PloterrAll` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrall <binomcikit.err.plots.ploterrall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrWD` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrwd <binomcikit.err.plots.ploterrwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrSC` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrsc <binomcikit.err.plots.ploterrsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrAS` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterras <binomcikit.err.plots.ploterras>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrTW` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrtw <binomcikit.err.plots.ploterrtw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrLT` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrlt <binomcikit.err.plots.ploterrlt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrLR` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrlr <binomcikit.err.plots.ploterrlr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrBA` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrba <binomcikit.err.plots.ploterrba>` | ggplot2 → plotnine; returns a plotnine `ggplot`; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |
| `PloterrEX` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrex <binomcikit.err.plots.ploterrex>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/511.Error-Failure_LimitBased_ADJ_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/511.Error-Failure_LimitBased_ADJ_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `errAWD` | {py:mod}`err.adj_all <binomcikit.err.adj_all>` | {py:func}`errawd <binomcikit.err.adj_all.errawd>` | lowercased name; pandas `DataFrame` |
| `errASC` | {py:mod}`err.adj_all <binomcikit.err.adj_all>` | {py:func}`errasc <binomcikit.err.adj_all.errasc>` | lowercased name; pandas `DataFrame` |
| `errAAS` | {py:mod}`err.adj_all <binomcikit.err.adj_all>` | {py:func}`erraas <binomcikit.err.adj_all.erraas>` | lowercased name; pandas `DataFrame` |
| `errALR` | {py:mod}`err.adj_all <binomcikit.err.adj_all>` | {py:func}`erralr <binomcikit.err.adj_all.erralr>` | lowercased name; pandas `DataFrame` |
| `likelhd` | — | *(no standalone function)* | internal R helper — folded into the shared Python engine; lowercased name; pandas `DataFrame` |
| `loglik` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `loglik.optim` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `errATW` | {py:mod}`err.adj_all <binomcikit.err.adj_all>` | {py:func}`erratw <binomcikit.err.adj_all.erratw>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `errALT` | {py:mod}`err.adj_all <binomcikit.err.adj_all>` | {py:func}`erralt <binomcikit.err.adj_all.erralt>` | lowercased name; pandas `DataFrame` |
| `errAAll` | {py:mod}`err.adj_all <binomcikit.err.adj_all>` | {py:func}`erraall <binomcikit.err.adj_all.erraall>` | lowercased name; pandas `DataFrame` |

### [`R/512.Error-Failure_LimitBased_ADJ_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/512.Error-Failure_LimitBased_ADJ_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PloterrAWD` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrawd <binomcikit.err.plots.ploterrawd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrASC` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrasc <binomcikit.err.plots.ploterrasc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrAAS` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterraas <binomcikit.err.plots.ploterraas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrALT` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterralt <binomcikit.err.plots.ploterralt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrATW` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterratw <binomcikit.err.plots.ploterratw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrALR` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterralr <binomcikit.err.plots.ploterralr>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrAAll` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterraall <binomcikit.err.plots.ploterraall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/521.Error-Failure_Limitbased_CC_All.R`](https://github.com/RajeswaranV/proportion/blob/master/R/521.Error-Failure_Limitbased_CC_All.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `errCWD` | {py:mod}`err.cc_all <binomcikit.err.cc_all>` | {py:func}`errcwd <binomcikit.err.cc_all.errcwd>` | lowercased name; pandas `DataFrame` |
| `errCSC` | {py:mod}`err.cc_all <binomcikit.err.cc_all>` | {py:func}`errcsc <binomcikit.err.cc_all.errcsc>` | lowercased name; pandas `DataFrame` |
| `errCAS` | {py:mod}`err.cc_all <binomcikit.err.cc_all>` | {py:func}`errcas <binomcikit.err.cc_all.errcas>` | lowercased name; pandas `DataFrame` |
| `errCLT` | {py:mod}`err.cc_all <binomcikit.err.cc_all>` | {py:func}`errclt <binomcikit.err.cc_all.errclt>` | lowercased name; pandas `DataFrame` |
| `lgiti` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `errCTW` | {py:mod}`err.cc_all <binomcikit.err.cc_all>` | {py:func}`errctw <binomcikit.err.cc_all.errctw>` | lowercased name; pandas `DataFrame` |
| `f1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `f2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `errCAll` | {py:mod}`err.cc_all <binomcikit.err.cc_all>` | {py:func}`errcall <binomcikit.err.cc_all.errcall>` | lowercased name; pandas `DataFrame` |

### [`R/522.Error-Failure_Limitbased_CC_All_Graph.R`](https://github.com/RajeswaranV/proportion/blob/master/R/522.Error-Failure_Limitbased_CC_All_Graph.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `PloterrCAll` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrcall <binomcikit.err.plots.ploterrcall>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrCWD` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrcwd <binomcikit.err.plots.ploterrcwd>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrCAS` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrcas <binomcikit.err.plots.ploterrcas>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrCLT` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrclt <binomcikit.err.plots.ploterrclt>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrCTW` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrctw <binomcikit.err.plots.ploterrctw>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |
| `PloterrCSC` | {py:mod}`err.plots <binomcikit.err.plots>` | {py:func}`ploterrcsc <binomcikit.err.plots.ploterrcsc>` | ggplot2 → plotnine; returns a plotnine `ggplot`; lowercased name; pandas `DataFrame` |

### [`R/523.Error-Failure_LimitBased_GENERAL.R`](https://github.com/RajeswaranV/proportion/blob/master/R/523.Error-Failure_LimitBased_GENERAL.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `errGEN` | {py:mod}`err.general <binomcikit.err.general>` | {py:func}`errgen <binomcikit.err.general.errgen>` | lowercased name; pandas `DataFrame` |

## 6xx — Bayesian


### [`R/611.Empirical.R`](https://github.com/RajeswaranV/proportion/blob/master/R/611.Empirical.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `empericalBA` | {py:mod}`bayes.empirical <binomcikit.bayes.empirical>` | {py:func}`empericalba <binomcikit.bayes.empirical.empericalba>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |

### [`R/612.Empirical_x.R`](https://github.com/RajeswaranV/proportion/blob/master/R/612.Empirical_x.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `empericalBAx` | {py:mod}`bayes.empirical <binomcikit.bayes.empirical>` | {py:func}`empericalbax <binomcikit.bayes.empirical.empericalbax>` | `stats::rbeta` → NumPy `default_rng`; adds `seed=`; not draw-for-draw identical to R; uses SciPy HPD (`_hpd.hpd_beta`); lowercased name; pandas `DataFrame` |

### [`R/621.Poster_Predictive.R`](https://github.com/RajeswaranV/proportion/blob/master/R/621.Poster_Predictive.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `probPRE` | {py:mod}`bayes.predictive <binomcikit.bayes.predictive>` | {py:func}`probpre <binomcikit.bayes.predictive.probpre>` | lowercased name; pandas `DataFrame` |

### [`R/622.Poster_Predictive_x.R`](https://github.com/RajeswaranV/proportion/blob/master/R/622.Poster_Predictive_x.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `probPREx` | {py:mod}`bayes.predictive <binomcikit.bayes.predictive>` | {py:func}`probprex <binomcikit.bayes.predictive.probprex>` | lowercased name; pandas `DataFrame` |

### [`R/631.BayesFactors.R`](https://github.com/RajeswaranV/proportion/blob/master/R/631.BayesFactors.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `hypotestBAF1` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf1 <binomcikit.bayes.bayesfactors.hypotestbaf1>` | lowercased name; pandas `DataFrame` |
| `hypotestBAF2` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf2 <binomcikit.bayes.bayesfactors.hypotestbaf2>` | lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF3` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf3 <binomcikit.bayes.bayesfactors.hypotestbaf3>` | lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF4` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf4 <binomcikit.bayes.bayesfactors.hypotestbaf4>` | lowercased name; pandas `DataFrame` |
| `bet0` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet01` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet11` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF5` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf5 <binomcikit.bayes.bayesfactors.hypotestbaf5>` | lowercased name; pandas `DataFrame` |
| `bet0` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet01` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet11` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF6` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf6 <binomcikit.bayes.bayesfactors.hypotestbaf6>` | lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |

### [`R/632.BayesFactors_x.R`](https://github.com/RajeswaranV/proportion/blob/master/R/632.BayesFactors_x.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `hypotestBAF1x` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf1x <binomcikit.bayes.bayesfactors.hypotestbaf1x>` | lowercased name; pandas `DataFrame` |
| `hypotestBAF2x` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf2x <binomcikit.bayes.bayesfactors.hypotestbaf2x>` | lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF3x` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf3x <binomcikit.bayes.bayesfactors.hypotestbaf3x>` | lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF4x` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf4x <binomcikit.bayes.bayesfactors.hypotestbaf4x>` | lowercased name; pandas `DataFrame` |
| `bet0` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet01` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet11` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF5x` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf5x <binomcikit.bayes.bayesfactors.hypotestbaf5x>` | lowercased name; pandas `DataFrame` |
| `bet0` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet01` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet11` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `hypotestBAF6x` | {py:mod}`bayes.bayesfactors <binomcikit.bayes.bayesfactors>` | {py:func}`hypotestbaf6x <binomcikit.bayes.bayesfactors.hypotestbaf6x>` | lowercased name; pandas `DataFrame` |
| `bet1` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
| `bet2` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |

### [`R/641.PosteriorProbs.R`](https://github.com/RajeswaranV/proportion/blob/master/R/641.PosteriorProbs.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `probPOS` | {py:mod}`bayes.posterior <binomcikit.bayes.posterior>` | {py:func}`probpos <binomcikit.bayes.posterior.probpos>` | lowercased name; pandas `DataFrame` |
| `bet` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |

### [`R/642.PosteriorProbs_x.R`](https://github.com/RajeswaranV/proportion/blob/master/R/642.PosteriorProbs_x.R)

| R function | Python file | Python function | R → Py changes |
|---|---|---|---|
| `probPOSx` | {py:mod}`bayes.posterior <binomcikit.bayes.posterior>` | {py:func}`probposx <binomcikit.bayes.posterior.probposx>` | lowercased name; pandas `DataFrame` |
| `bet` | — | *(no standalone function)* | folded into the shared Python engine (no standalone function); lowercased name; pandas `DataFrame` |
